import React from 'react';
import ReactDOM from 'react-dom/client';
import StoreTracker from './StoreTracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StoreTracker />
  </React.StrictMode>
);
